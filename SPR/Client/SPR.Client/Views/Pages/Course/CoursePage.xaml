<UserControl x:Class="SPR.Client.Views.Pages.Course.CoursePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:SPR.Client.Views.Pages.Course"
      xmlns:resources="clr-namespace:SPR.Client.Assets"
      mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" Height="Auto" Width="Auto">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="1" Grid.Row="0" HorizontalAlignment="Center" FontSize="32" Text="{x:Static resources:text_en.CourseTitle}" VerticalAlignment="Center"/>
        <ListView Grid.Row="1" Grid.Column="1">
            <GridView>
            </GridView>
        </ListView>
        <ContentControl Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" x:Name="courseFrame"></ContentControl>
    </Grid>
</UserControl>
